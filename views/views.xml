<flectra>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="metro_crm_module.list">
      <field name="name">metro_crm_module list</field>
      <field name="model">metro_crm_module.metro_crm_module</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="metro_crm_module.action_window">
      <field name="name">metro_crm_module window</field>
      <field name="res_model">metro_crm_module.metro_crm_module</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="metro_crm_module.action_server">
      <field name="name">metro_crm_module server</field>
      <field name="model_id" ref="model_metro_crm_module_metro_crm_module"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="metro_crm_module" id="metro_crm_module.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="metro_crm_module.menu_1" parent="metro_crm_module.menu_root"/>
    <menuitem name="Menu 2" id="metro_crm_module.menu_2" parent="metro_crm_module.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="metro_crm_module.menu_1_list" parent="metro_crm_module.menu_1"
              action="metro_crm_module.action_window"/>
    <menuitem name="Server to list" id="metro_crm_module" parent="metro_crm_module.menu_2"
              action="metro_crm_module.action_server"/>
    -->
  </data>
</flectra>